FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      surfaceFeaturesDict;
}

// Triangulated surface format; we keep the STL under constant/geometry
surfaceFormat   triSurfaceMesh;

// Input geometry inside constant/geometry
surfaces
(
    "city_buildings.stl"
);

// Capture sharp building edges; higher angle keeps fewer features
includedAngle   150;

// Extra outputs not needed for snappy
writeObj        no;
